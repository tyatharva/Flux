# Ninth-pass VALIDATION records — NOT the corpus

Two records from the 2026-08-30 ninth pass (`case_2023111718`, two windows). Same 30 m
grid and identical array shapes as the corpus, which is why this directory is no longer
called `pairs_npz`: `DEPLOY.md` §C6 rsyncs eight machines into a directory of that name at
the repo root, and these would have been swept in silently.

The corpus is `corpus/pairs_npz/` (1368 records) and its index is `corpus/INDEX.json`.
